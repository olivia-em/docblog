---
layout: layouts/base.njk
templateClass: tmpl-home
tags:
  - class
  - fall2024
---

{# This is the template for the Posts page–it lists the posts in the collection #}

<div class="wrapper home">
  <div>
    <h3 style="text-align: center;">
      Applications
    </h3>
    <ul class="projects">
      {%- for app in collections.apps -%}
        <li{% if page.url == app.url %} aria-current="page"{% endif %}> <a href='{{ app.url }}'>{{ app.data.title }}</a></li>
      {%- endfor -%}
    </ul>
  </div>
</div>